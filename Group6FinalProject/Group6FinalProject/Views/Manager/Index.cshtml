@{
    Layout = "_Layout";
    ViewData["Title"] = "Manager Dashboard";
}

@section Styles {
    <link rel="stylesheet" type="text/css" href="~/css/Manager.css" />
}

<h2>Welcome, Manager</h2>
<p>What would you like to do today?</p>

<div class="option-menu">
    <a class="btn btn-primary" href="@Url.Action("AddMovie", "Movies")">Add Movie</a>
    <a class="btn btn-primary" href="@Url.Action("CustomerAccounts", "Accounts")">Customer Accounts</a>
    <a class="btn btn-primary" href="@Url.Action("SellTickets", "Tickets")">Sell Tickets</a>
    <a class="btn btn-primary" href="@Url.Action("EmployeeManager", "Employee")">Employee Manager</a>
    <a class="btn btn-primary" href="@Url.Action("MovieReviewManager", "Reviews")">Movie Review Manager</a>
    <a class="btn btn-primary" href="@Url.Action("MovieSearch", "Search")">Movie Search</a>
    <a class="btn btn-primary" href="@Url.Action("ScheduleMovies", "Schedule")">Schedule Movies</a>
    <a class="btn btn-primary" href="@Url.Action("Reports", "Reporting")">Reports</a>
    <a class="btn btn-primary" href="@Url.Action("EditManagerProfile", "Profile")">Edit Manager Profile</a>
</div>
